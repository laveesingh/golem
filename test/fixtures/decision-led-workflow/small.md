You are authorized to coordinate this named fixture spec through SDD.

Spec: SMALL-bounded-badge — add one static status badge to this project's README.
It touches no code, no consumers, and one file.

Ground it, then make the two light choices (badge text; badge host) directly in the
spec with a one-line implication each. Report: what you decided, why, and what you
did NOT create (e.g. no workshop) and why.
