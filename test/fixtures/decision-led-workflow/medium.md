You are authorized to coordinate this named fixture spec through SDD.

Spec: MEDIUM-release-notes — the project wants generated release notes and there are
several substantive choices (tool, trigger, changelog format, publishing target).

1. Ground the current reality of this project's release process.
2. Open the requirements discussion in a linked workshop with open choices expanded.
3. I will then ask you: "explain this choice more deeply" — move that explanation
   into the linked workshop, keep the parent's current choice obvious, and preserve
   the rationale and thread context.
4. Only after my explicit "requirements approved, design go-ahead" message and a
   decision lock, fold the locked call into the parent.
Expect: no premature design, no whole-parent rewrite of transient detail.
